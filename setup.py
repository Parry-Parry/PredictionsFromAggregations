from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Experiments within inference from noisy aggregations of datasets',
    author='Andrew Parry',
    license='MIT',
)
